import { Pressable, StyleSheet, Text, View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import { useRouter } from 'expo-router'; import { AuthShell, authStyles } from '@/components/AuthShell'; import { colors } from '@/lib/theme';
export default function Verify(){const router=useRouter();return <AuthShell title="Check your email" subtitle="We have sent a secure link to your inbox. Click the verification link inside to safely activate your account."><View style={s.icon}><Ionicons name="mail-open-outline" size={34} color={colors.green}/></View><Pressable onPress={()=>router.replace('/login')} style={authStyles.button}><Text style={authStyles.buttonText}>Return to Sign In</Text></Pressable><Text style={authStyles.footer}>Didn't receive the email? <Text style={authStyles.link}>Resend link</Text></Text></AuthShell>}
const s=StyleSheet.create({icon:{width:68,height:68,borderRadius:34,backgroundColor:'rgba(92,169,135,.12)',alignItems:'center',justifyContent:'center',alignSelf:'center',marginBottom:18}});
