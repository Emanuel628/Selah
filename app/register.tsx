import { useState } from 'react'; import { Pressable, Text, TextInput } from 'react-native'; import { useRouter } from 'expo-router'; import { AuthShell, authStyles as s } from '@/components/AuthShell';
export default function Register(){const router=useRouter();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');return <AuthShell title="Begin your garden" subtitle="A distraction-free home for scriptural study."><Text style={s.label}>Full Name</Text><TextInput value={name} onChangeText={setName} style={s.input}/><Text style={s.label}>Email Address</Text><TextInput value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" style={s.input}/><Text style={s.label}>Create Password</Text><TextInput value={password} onChangeText={setPassword} secureTextEntry style={s.input}/><Pressable onPress={()=>router.replace('/(tabs)')} style={s.button}><Text style={s.buttonText}>Create Free Account</Text></Pressable><Text style={s.footer}>Already have an account? <Text onPress={()=>router.replace('/login')} style={s.link}>Sign in</Text></Text></AuthShell>}
