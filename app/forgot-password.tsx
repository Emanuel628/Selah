import { useState } from 'react'; import { Pressable, Text, TextInput } from 'react-native'; import { useRouter } from 'expo-router'; import { AuthShell, authStyles as s } from '@/components/AuthShell';
export default function Forgot(){const router=useRouter();const [email,setEmail]=useState('');return <AuthShell title="Recover your peace" subtitle="Enter your email to receive password reset instructions."><Text style={s.label}>Email Address</Text><TextInput value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" placeholder="you@example.com" placeholderTextColor="#6f827c" style={s.input}/><Pressable onPress={()=>router.replace('/login')} style={s.button}><Text style={s.buttonText}>Send Reset Link</Text></Pressable><Text style={s.footer}><Text onPress={()=>router.back()} style={s.link}>← Back to Sign In</Text></Text></AuthShell>}
